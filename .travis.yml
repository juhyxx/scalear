language: node_js
node_js:
- '0.12'
sudo: required
branches:
  only:
  - master
cache:
  directories:
  - node_modules
  - bower_components
before_install:
- npm install -g bower
before_script:
  - npm install -g brunch
install:
- bower install
- npm install
script:
- brunch build --production
